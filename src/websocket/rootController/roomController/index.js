const classMeta = require(load('config').path.decorators).classMeta

module.exports = 
    @classMeta
    class RoomController {
        room;

        constructor ({room}) {

        }


        on () {
            
        }
    }